
//set defaults
- if (!err) var err = ""
- if (!name) var name = ""

div#centerDiv
	h2 Captain #{name}, 
	h2 please select your ship:
	select#shipSelect
		each ship, i in ships
			option(value="#{ship._id}") #{ship.name}
	a(href="#", onclick="changePage('/start/newShip')") New Ship
	a(href="#", onclick="changePage('/start/selectShip','POST',{ship: $('#shipSelect').val()})") Make it so

	p #{err}
	div#nameInstructions(style="display:none")
		//make this say 'tap to continue' for iOS
		p (press Enter to continue)